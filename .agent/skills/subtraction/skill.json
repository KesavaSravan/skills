{
  "name": "subtraction",
  "version": "1.0.0",
  "description": "Calculates the difference between two numeric inputs.",
  "entry": "skill.py",
  "parameters": {
    "type": "object",
    "properties": {
      "a": {
        "type": "number",
        "description": "The first number (minuend)."
      },
      "b": {
        "type": "number",
        "description": "The second number to subtract (subtrahend)."
      }
    },
    "required": ["a", "b"]
  }
}
